// Added a constant here for the api so that its easily changeable if needed
const noroffAPIURL = "https://v2.api.noroff.dev";

// use a different constant for usage so that the main constant is not changed by mistake
const apiUrl = noroffAPIURL;

// If this is changed it will display a different blog (maybe the user want a new blog or something)
const blogName = "oyvind";

export { apiUrl, blogName};
